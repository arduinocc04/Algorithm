#include <iostream>
#include <string>
int T, n;
std::string s;
int main() {
    std::cin.sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);
    std::cin >> T;
    while(T--) {
        std::cin >> s;
        
    }
    return 0;
}
