#include <cstdio>
#include <vector>
int n, m, i, j, b;
std::vector<int> graph[210];
int main() {
    scanf("%d %d", &n, &m);
    for(int l = 0; l < m; l++) {
        scanf("%d %d %d", &i, &j, &b);
    }
    return 0;
}
