from collections import deque
keyword = input()
a = input()
queue = deque()
for i in a:
    queue.append(deque)
front = ''
back = ''
fqueue = deque()
bqueue = deque()
tmp = deque()
while len(queue):
    a = queue.pop()
    b = queue.popleft()
    fqueue.append(a)
    bqueue.append(b)
    if len(a) == keyword:
        for i in range(len(fqueue)):
