#include <cstdio>
#include <algorithm>

int h, w, n, a, b;
int map[3010][3010];

bool calc(int sr, int sc, int er, int ec) {

}

int main() {
    scanf("%d %d %d", &h, &w, &n);
    for(int i = 0; i < n; i++) {
        scanf("%d %d", &a, &b);
        map[a][b] = 1;
    }
}
